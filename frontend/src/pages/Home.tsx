import { Component } from "solid-js";
import Auth from "../utils/Auth";

const Home: Component = () => {
    return (
        <div>
            Home Page
            <button>Logout</button>
        </div>
    );
};

export default Home;
