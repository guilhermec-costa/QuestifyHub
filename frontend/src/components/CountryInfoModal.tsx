import { Component } from "solid-js";

const CountryInfoModal: Component = () => {
    return (
        <div class="absolute z-10 w-2/5 bg-slate-300 rounded-sm left-[-30%] text-sm p-1">
            Improves search based on the region
        </div>
    );
};

export default CountryInfoModal;
